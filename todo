proton-wine-protonge
run?

logheeheeeee? adapted
facade obv, non dovrebbe esserci codice idk
prendi un exe, rendilo eseguibile
chain? if in list, if game, nothing then wine
factory method

template p23
ma no meglio strategy obv
p48

- Factory Method
��� Abstract Factory
��� Static Factory Method
��� Singleton
��� Fluent Interface
* Builder

public class TimeSpan {
 private TimeSpan(int seconds, int minutes) {
 // ...
 }
 public static TimeSpan ofSeconds(int seconds) {
 return new TimeSpan(seconds, 0);
 }
 public static TimeSpan ofMinutes(int minutes) {
 return new TimeSpan(0, minutes);
 }
}
p nice per builder

��� Si pu�� combinare builder e static factory method:
p76

name necessario
id, isaco etc opzionali

aggiungi se vuoi performance o compatibilit��


un iterator per lista di programmi e forse anche filtrata 
p29 iterator 

In questo corso, a scopo didattico, si consiglia di 
implementare degli iteratori
��� Anche dichiarando una propria interfaccia per gli 
iteratori
��� Per computazioni interne su collezioni si consiglia 
invece di usare gli stream

NO DECORATOR FA CAGARE

sus

Executable exe=new Executable(ExecutableBuilder.getBuilder().setName("wine").setPath("/usr/bin/wine").setArgs(new String[]{"/home/martin/Desktop/test.exe"}).build());

ma tipo un observer? con logger prob? o allo start

capire cosa deve fare executable, dividi i compiti
